hi


<div>
  <div id="map" style='width: 800px; height: 400px;'></div>
</div>

<script>
  function initMap() {
    var mapDiv = document.getElementById('map');

    var map = new google.maps.Map(mapDiv);

    var bounds = new google.maps.LatLngBounds();
    
    var infowindow = new google.maps.InfoWindow({
      content: "Click Content" 
    });
    
    var marker = new google.maps.Marker({
      position: {lat: <%= @latitude %>, lng:<%= @longitude %>}, 
      map: map,
      title: 'Hello World!',
      icon: 'http://maps.google.com/mapfiles/ms/icons/red-dot.png' 
    });
    
    marker.addListener('click', function() {
      infowindow.open(map, marker);
    });

  bounds.extend(new google.maps.LatLng(<%= @latitude %>,<%= @longitude %> ));

  map.fitBounds(bounds);
  };
  
</script>
<script src="https://maps.googleapis.com/maps/api/js?key=<AIzaSyD8RrOFB0dPsF-leqeFJdmX3yOvcQbfNyY&callback=initMap>"
async defer></script>


<form action="/send_message">
  <label for="message"> Personalize your message:</ label>
  <input id="message" name="user_input" placeholder="Hi, I'll be at <%= @street_address %>">

  <input name="user_id" type="hidden" value="<%= session.fetch(:user_id) %>">

  <input name="user_location" type="hidden" value="<%= @street_address %>">

  <button>
    Submit
  </button>

  </form>

